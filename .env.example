# Sanity (required)
NEXT_PUBLIC_SANITY_PROJECT_ID=mi2z88oe
NEXT_PUBLIC_SANITY_DATASET=tradelibrary

# Newsletter — Buttondown (recommended)
# Sign up at https://buttondown.com, create an API key under Settings → API.
# Buttondown handles double opt-in, unsubscribe links, and GDPR-friendly consent.
BUTTONDOWN_API_KEY=

# Alternative ESPs (not wired by default — swap logic in src/lib/newsletter.ts if needed):
# ConvertKit: https://convertkit.com — form/API with tags per download source
# Mailchimp: https://mailchimp.com — audience + merge fields for resource slug
